/**
 * Interrupteurs Vanta / mode présentation « dev » (Funky).
 * Mettre à `true` pour réactiver sans retoucher le reste du code.
 */
export const VANTA_NET_ENABLED = false

/** Toggle navbar beige ↔ dev (Funky On/Off) et mode présentation créa. */
export const PRESENTATION_DEV_MODE_ENABLED = false
